<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <!-- Tailwind CSS CDN (Primary) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'inter': ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <!-- Local CSS (Fallback) -->
    <link href="/stylesheets/output.css" rel="stylesheet" />
</head>

<body class="font-inter select-none">
    <nav class="w-full flex justify-between items-center px-[5vw] md:px-[10vw] h-28 md:mb-10">
        <h3 class="text-blue-400 font-bold text-3xl">Scatch</h3>
        <% var loggedIn=typeof loggedIn !=='undefined' ? loggedIn : true; %>
            <% if (loggedIn) { %>
                <div class="flex gap-3 md:gap-6">
                    <a href="/shop" class="text-gray-800 hover:text-yellow-500 transition-colors duration-200">
                        <i class="ri-shopping-bag-line text-2xl font-bold"></i>
                    </a>
                    <a href="/cart" class="text-gray-800 hover:text-yellow-500 transition-colors duration-200">
                        <i class="ri-shopping-cart-line text-2xl font-bold"></i>
                    </a>
                    <a href="/account" class="text-gray-800 hover:text-yellow-500 transition-colors duration-200"><i
                            class="ri-account-circle-fill text-2xl font-bold"></i></a>
                </div>
                <% } %>
    </nav>